apiVersion: v1
kind: Service
metadata:
  name: linux-devops-dashboard
  labels:
    app: linux-devops-dashboard
spec:
  type: ClusterIP
  selector:
    app: linux-devops-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
