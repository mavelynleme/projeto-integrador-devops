apiVersion: v1
kind: Secret
metadata:
  name: linux-devops-dashboard-secret-example
  labels:
    app: linux-devops-dashboard
  annotations:
    description: "Example only. Replace placeholder values before using real secrets."
type: Opaque
stringData:
  EXAMPLE_TOKEN: "replace-with-a-real-secret-if-needed"
  EXAMPLE_PASSWORD: "replace-with-a-real-secret-if-needed"
